{
    "$schema": "https://docs.openclaw.ai/schema/openclaw.json",
    "_comment": "Hardened OpenClaw configuration — generated by security scripts",
    "gateway": {
        "auth": {
            "mode": "token"
        },
        "bind": "loopback",
        "tailscale": "serve",
        "nodes": {
            "_comment": "Only block dangerous system-level commands. The Mac is dedicated to the bot with its own Apple account, so calendar, browser, contacts, reminders, SMS, and screen capture are allowed.",
            "denyCommands": [
                "system.run",
                "system.which"
            ]
        }
    },
    "models": {
        "providers": {
            "ollama-local": {
                "type": "openai",
                "baseUrl": "http://127.0.0.1:11434/v1",
                "models": [
                    "qwen2.5:32b-instruct-q4_k_m",
                    "hermes3:8b"
                ]
            },
            "openrouter-cloud": {
                "type": "openrouter",
                "_comment": "Fallback for complex tasks that exceed local model capabilities"
            }
        },
        "defaults": {
            "chat": "ollama-local/qwen2.5:32b-instruct-q4_k_m"
        }
    },
    "logging": {
        "redactSensitive": "tools"
    },
    "memory": {
        "backend": "sqlite-vec",
        "_comment": "Native embedding-based memory. Cognee Graph RAG is a future enhancement."
    }
}
