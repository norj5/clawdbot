{
    "$schema": "https://docs.openclaw.ai/schema/openclaw.json",
    "_comment": "Hardened OpenClaw configuration — Zero Trust / Control-Plane Integrity",

    "gateway": {
        "auth": {
            "mode": "token"
        },
        "bind": "loopback",
        "tailscale": "serve",
        "nodes": {
            "_comment": "Block dangerous system-level commands. Calendar, browser, contacts, reminders, SMS, and screen capture are allowed (Mac is dedicated to the bot).",
            "denyCommands": [
                "system.run",
                "system.which"
            ]
        }
    },

    "commands": {
        "_comment": "Disable commands that allow the bot to modify its own runtime from chat.",
        "config": false,
        "debug": false,
        "bash": false
    },

    "tools": {
        "elevated": {
            "_comment": "Elevated mode allows bypassing tool restrictions. Keep disabled.",
            "enabled": false
        },
        "exec": {
            "_comment": "All exec runs in sandbox, never on the host. Allowlist mode = only pre-approved commands.",
            "host": "sandbox",
            "security": "allowlist",
            "ask": "always"
        }
    },

    "hooks": {
        "internal": {
            "entries": {
                "soul-evil": {
                    "_comment": "CRITICAL: This hook replaces SOUL.md with SOUL_EVIL.md. Must stay disabled.",
                    "enabled": false
                }
            }
        }
    },

    "plugins": {
        "_comment": "Empty allowlist = no plugins load. Add trusted plugin IDs explicitly.",
        "allow": []
    },

    "channels": {
        "telegram": {
            "_comment": "Prevent the bot from rewriting its own config via Telegram commands.",
            "configWrites": false
        }
    },

    "models": {
        "providers": {
            "ollama-local": {
                "type": "openai",
                "baseUrl": "http://127.0.0.1:11434/v1",
                "models": [
                    "qwen2.5:32b-instruct-q4_k_m",
                    "hermes3:8b"
                ]
            },
            "openrouter-cloud": {
                "type": "openrouter",
                "_comment": "Fallback for complex tasks that exceed local model capabilities"
            }
        },
        "defaults": {
            "chat": "ollama-local/qwen2.5:32b-instruct-q4_k_m"
        }
    },

    "logging": {
        "redactSensitive": "tools"
    },

    "memory": {
        "backend": "sqlite-vec",
        "_comment": "Native embedding-based memory. Cognee Graph RAG is a future enhancement."
    }
}
